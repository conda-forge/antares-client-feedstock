{% set name = "antares-client" %}
{% set version = "1.12.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 7b92dda727b63ff4a063a8886a9653784ccd2109e7f59045f4a94cd60081fce8

build:
  entry_points:
    - antares=antares_client.cli:entry_point
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pytest-runner
    - pip
    - setuptools >=40.8.0
  run:
    - python >={{ python_min }}
    - astropy-base
    - bson
    - click
    - python-confluent-kafka ==1.7.0
    - marshmallow >=3.6
    - marshmallow-jsonapi >=0.23
    - numpy
    - pandas
    - pytest
    - requests
    - requests-mock
    - typing_extensions
    - astropy-healpix

test:
  imports:
    - antares_client
  commands:
    - pip check
    - antares --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://gitlab.com/nsf-noirlab/csdc/antares/client/
  summary: A light-weight client for receiving alerts from ANTARES.
  license: MIT
  license_file: LICENSE
  dev_url: https://gitlab.com/nsf-noirlab/csdc/antares/client
  doc_url: https://nsf-noirlab.gitlab.io/csdc/antares/client/

extra:
  recipe-maintainers:
    - davner
